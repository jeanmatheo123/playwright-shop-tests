{
  "name": "playwright-shop-tests",
  "version": "1.0.0",
  "description": "End-to-end test suite for the Sauce Demo sample app, built with Playwright and TypeScript.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3"
  }
}
